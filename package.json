{
  "name": "mrghiche-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd next-migrated && next dev",
    "build": "cd next-migrated && next build",
    "start": "cd next-migrated && next start"
  }
}